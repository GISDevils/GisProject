# .travis.yml
branches:
  only:
    - master
    - backend  # FIXME remove at merging.

language: python

python:
  - "2.7"

env:
  - DJANGO=1.5

install:
  - pip install -r requirements.txt

script:
  - python wsgi/GIS/manage.py test
