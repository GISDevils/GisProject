# .travis.yml
branches:
  only:
    - master
    - backend  # FIXME remove at merging.

language: python

before_script:
  - mysql -e 'create database test_gis;'

python:
  - "2.7"

env:
  - DJANGO=1.5

install:
  - pip install -r requirements.txt

script:
  - python GIS/manage.py test
